# Runbook

This repository does not ship an operator-specific production runbook.

Use:

- `README.md` for the current project state
- `DEPLOY.md` for reproducible setup commands
- `docs/TECHNICAL_EVIDENCE.md` for what is demonstrably implemented

Create an environment-specific runbook only when a real deployment exists.
